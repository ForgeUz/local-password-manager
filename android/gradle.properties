org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template. Required by this Flutter
# version's Gradle plugin; removing it breaks the AGP cast. Keep it.
android.newDsl=false
# builtInKotlin=false forces the legacy Kotlin Gradle Plugin path, which produces
# the (non-fatal) "plugins apply KGP" warning. Enabling built-in Kotlin here breaks
# this Flutter/AGP combo, so the warning is accepted. Upgrade Flutter to remove it.
android.builtInKotlin=false
